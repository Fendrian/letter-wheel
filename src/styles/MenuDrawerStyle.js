import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    borderRadius: 4,
    height: 300,
    width: 300,
  },
  container: {
    borderRadius: 4,
    alignItems: 'center',
    backgroundColor: '#123',
    flex: 1,
    justifyContent: 'center',
  },
});
