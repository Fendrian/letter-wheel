import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  text: {
    fontSize: 20,
  },
  wrapper: {
    alignContent: 'center',
    flex: 1,
    justifyContent: 'center',
  },
});
